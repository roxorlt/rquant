# React 投研平台完成进度

本文件只记录已经有行为证据的交付状态，功能范围与验收以 [平台方向决定](2026-09-26-react-platform-decision.md)、CC 的可点击原型、`frontend-plan-v2.md` 第 4 节及差距表为准。原型中的可见操作都须接通真实行为；差距表中的「缺」「部分」，以及 M4、M12 虽标「已有」但列出的缺口，都在最终范围内。M11 实盘交易排除。

## 完成标准

- 页面按原型的使用路径工作；空态、加载、错误、窄屏、键盘和中文文案遵守 `web/AGENTS.md`。技术编号和服务代号不放在页面正文。
- React 负责交互和展示；有类型的网页 API 读取固定的 Serving 数据代；计算复用纯领域函数；写操作走受控命令。按钮不能仅演示点击效果。
- 每个 Streamlit 单元只有在对应真实使用路径、数据及操作经验证后才记录替代和回退并停用。新平台不再向 Streamlit 增加产品功能。
- 标记「完成」须同时有代码、相关行为验证和发布状态；本地候选不等同于生产可用。

## 当前增量（2026-09-27）

| 范围 | 当前状态 | 证据与下一步 |
|---|---|---|
| M0 外框、报告入口，M1 里程碑总览 / 系统健康 / 市场全景 / 个股入口 | 本地候选，未正式切流 | PR #315；聚焦测试与浏览器检查通过，完整 CI 运行中。正式 Serving 尚未发布数据代。 |
| C4.1 手工选股、26 条规则目录、逐条命中、分页、个股详情 | 本地候选，**部分** | 已复用原规则和只读数据代；现有 universe 缺历史偏移及窗口列，不能声称 26 条规则的全部参数可运行。 |
| C4.2 加权排名 | 本地候选，未发布 | 先筛后排、缺值靠后、权重比例折算、前 N 和稳定分页已验证；`RETURN_20D_PCT[0]` 是可选发布列，旧数据代不显示。 |
| C1.1 数据目录与字段字典 | 本地候选，**部分** | 23 份契约的静态目录、字段说明、只读 API 和响应式页面已做聚焦验证；真实样例行仍须由研究产物发布。 |

## 最终能力台账

| 模块 | 尚须完成的范围 |
|---|---|
| M1 数据中心 | C1.1 样例与目录；C1.2 审计/覆盖率；C1.3 回补计划与受控执行；C1.4 PIT 财务数据。 |
| M2 研究环境 | 研究 SDK、只读查询、Notebook 或等价研究环境。 |
| M3 因子研究 | 因子定义、检验、分组/衰减/换手、持续跟踪。 |
| M4 选股与池子 | 补齐全部规则参数的数据；一句话条件、基本面与盘中条件、公式导入、可编辑节点画布、池子进出记录、加入盯盘。 |
| M5 策略 | 受控模板、版本与详情、阶段及晋级证据。 |
| M6 回测 | 通用日线组合、基准、统一撮合执行。 |
| M7 绩效 | 净值/回撤、标准风险收益指标、月度与基准对照、可导出报告。 |
| M8 实验 | 实验登记写入、参数可视化、比较和过拟合指标。 |
| M9 组合风控 | 权重规则、组合回撤、行业暴露。 |
| M10 模拟盘 | 账户/持仓/指令/净值、对账和回测区间对照。 |
| M11 实盘交易 | 不做。 |
| M12 监控告警 | 规则配置、时间线与确认、经批准的正式推送切换。 |
| M13 调度任务 | 总览、日志、受控立即运行与研究任务操作。 |
| M14 AI 辅助 | 生成→预览→回测、结果解读、公告/研报摘要、画布自然语言修改。 |
| M15 协作权限 | 用户角色、操作记录、自包含只读结果分享。 |
| M16 运维健康 | 新运行时可视化、分层状态与可解释的异常入口。 |

每次发布列车只更新已验证的行；未完成能力继续留在台账里。部署和生产数据变更仍走项目单独授权规则。
