"""✍️ 沙盒因子与信号计算（空壳，W2 脚手架）。

## 职责

只写「从已登记的字段算出信号」这一件事：

- 输入是 `data_dictionary.md` 里登记过的字段，参数从 `config.yaml` 读；
- 输出是信号/因子值，不含任何成交假设；
- 每个函数对应 `hypothesis.md` 里一张假设卡的「信号定义」栏，函数 docstring 写明卡号。

## 不属于这里的东西（规格 §7 权限边界）

- **成交价、手续费、印花税、过户费、滑点、排队模拟** —— 属于回测底座，不在沙盒；
- **涨跌停 / 停牌 / 临停的可成交判定** —— 同上；
- **仓位与资金约束** —— 同上；
- **VP 本身的计算** —— 用 `rquant.vp.engine`，🔒 只读。要改引擎就走人工 review，
  改完重跑全部历史实验并在 `experiment_log.csv` 标注引擎版本变更。

阈值一律从 `config.yaml` 读，不在本文件写死数字：规格 §10 打 ⚠️ 的参数在 P0 结束前
都不得写死进代码。
"""

from __future__ import annotations
